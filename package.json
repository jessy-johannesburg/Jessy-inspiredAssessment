{
  "name": "cucumber-ts-playwright",
  "version": "1.0.0",
  "scripts": {
    "test": "cucumber-js --profile default",
    "cucumber_test": "cucumber-js --config=cucumber.json",
    "web_cucumber_test": "cucumber-js --config=cucumber.json --tags \"@BBCFeature\""
  },
  "dependencies": {
    "@cucumber/cucumber": "^9.6.0",
    "@playwright/test": "^1.42.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
